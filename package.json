{
    "name": "sri-ram-fashion-app",
    "version": "1.0.0",
    "description": "Sri Ram Fashions App",
    "scripts": {
        "start": "cd backend && npm start",
        "build": "npm install --prefix backend && npm install --prefix frontend-new && npm run build --prefix frontend-new",
        "build:frontend": "cd frontend-new && npm install && npm run build",
        "install:backend": "cd backend && npm install"
    },
    "engines": {
        "node": "20.x"
    }
}
