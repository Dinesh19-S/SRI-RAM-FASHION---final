{
    "name": "sri-ram-fashion-app",
    "version": "1.0.0",
    "description": "Sri Ram Fashions App",
    "scripts": {
        "start": "cd backend && npm start",
        "build": "cd backend && npm install && cd ../frontend-new && npm install && npx vite build --outDir ../dist",
        "build:frontend": "cd frontend-new && npm install && npm run build",
        "install:backend": "cd backend && npm install"
    },
    "engines": {
        "node": "20.x"
    }
}
